<!--
     General purpose build script for web applications and web services,
     including enhanced support for deploying directly to a Tomcat 4
     based server.

     This build script assumes that the source code of your web application
     is organized into the following subdirectories underneath the source
     code directory from which you execute the build script:

        src                  Java source code (and associated resource files)
                             to be compiled to the "WEB-INF/classes"
                             subdirectory of your web applicaiton.

        web                  Static HTML, JSP, and other content (such as
                             image files), including the WEB-INF subdirectory
                             and its configuration file contents.

     $Id: build.xml,v 1.14 2003/12/17 21:18:26 bmayhew Exp $
-->


<!-- A "project" describes a set of targets that may be requested
     when Ant is executed.  The "default" attribute defines the
     target which is executed if no specific target is requested,
     and the "basedir" attribute defines the current working directory
     from which Ant executes the requested task.  This is normally
     set to the current working directory.
-->

<project name="WebGoat" default="compile" basedir=".">

<!-- ===================== Property Definitions =========================== -->

<!--
  Each of the following properties are used in the build script.
  Values for these properties are set by the first place they are
  defined, from the following list:

  * Definitions on the "ant" command line (ant -Dfoo=bar compile).
  * Definitions from a "build.properties" file in the top level
    source directory of this application.
  * Definitions from a "build.properties" file in the developer's
    home directory.
  * Default definitions in this build.xml file.
  You will note below that property values can be composed based on the
  contents of previously defined properties.  This is a powerful technique
  that helps you minimize the number of changes required when your development
  environment is modified.  Note that property composition is allowed within
  "build.properties" files as well as in the "build.xml" script.
-->

  <property file="build.properties"/>
  <property file="${user.home}/build.properties"/>

<!-- ==================== File and Directory Names ======================== -->

<!--
  These properties generally define file and directory names (or paths) that
  affect where the build process stores its outputs.

  app.name             Base name of this application, used to
                       construct filenames and directories.
                       Defaults to "myapp".

  app.path             Context path to which this application should be
                       deployed (defaults to "/" plus the value of the
                       "app.name" property).

  app.version          Version number of this iteration of the application.

  build.home           The directory into which the "prepare" and
                       "compile" targets will generate their output.
                       Defaults to "build".

  catalina.home        The directory in which you have installed
                       a binary distribution of Tomcat 4.  This will
                       be used by the "deploy" target.

  dist.home            The name of the base directory in which
                       distribution files are created.
                       Defaults to "dist".

  manager.password     The login password of a user that is assigned the
                       "manager" role (so that he or she can execute
                       commands via the "/manager" web application)

  manager.url          The URL of the "/manager" web application on the
                       Tomcat installation to which we will deploy web
                       applications and web services.

  manager.username     The login username of a user that is assigned the
                       "manager" role (so that he or she can execute
                       commands via the "/manager" web application)
-->

  <property name="app.name"      		   value="WebGoat"/>
  <property name="app.path"      		   value="/${app.name}"/>
  <property name="app.version"   		   value="3.7"/>
  <property name="build.home"    		   value="${basedir}/build"/>
  <property name="catalina.home" 		   value="${basedir}/tomcat"/> <!-- UPDATE THIS! -->
  <property name="dist.home"     		   value="${basedir}/dist"/>
  <property name="web_inf.home"            value="${basedir}/build/WEB-INF"/>
  <property name="doc.home"      		   value="${basedir}/doc"/>
  <property name="manager.url"   		   value="http://localhost/manager"/>
  <property name="manager.username"   	   value="admin"/> 			<!-- UPDATE THIS! -->
  <property name="manager.password"   	   value="admin"/> 			<!-- UPDATE THIS! -->
  <property name="src.home"     		   value="${basedir}/src"/>
  <property name="web.home"      		   value="${basedir}/web"/>
  <property name="web_CBT.home"      	   value="${basedir}/web_CBT"/>
  <property name="zip_distributions.home"  value="${basedir}/zip_distributions"/>
  <property name="cygwin.home"             value="C:/cygwin"/>
  <property name="sourceforge.home"        value="C:/perforce/doug/depot/webgoat"/>

<!-- ================== Custom Ant Tomcat Task Definitions ================= -->

<!--
  These properties define custom tasks for the Ant build tool that interact
  with the "/manager" web application installed with Tomcat 4.  Before they
  can be successfully utilized, you must perform the following steps:

  - Copy the file "server/lib/catalina-ant.jar" from your Tomcat 4
    installation into the "lib" directory of your Ant installation.

  - Create a "build.properties" file in your application's top-level
    source directory (or your user login home directory) that defines
    appropriate values for the "manager.password", "manager.url", and
    "manager.username" properties described above.

  For more information about the Manager web application, and the functionality
  of these tasks, see <http://localhost:8080/tomcat-docs/manager-howto.html>.
-->

  <taskdef name="install" classname="org.apache.catalina.ant.InstallTask"/>
  <taskdef name="list"    classname="org.apache.catalina.ant.ListTask"/>
  <taskdef name="reload"  classname="org.apache.catalina.ant.ReloadTask"/>
  <taskdef name="remove"  classname="org.apache.catalina.ant.RemoveTask"/>

<!--  ==================== Compilation Control Options ==================== -->

<!--
  These properties control option settings on the Javac compiler when it
  is invoked using the <javac> task.

  compile.debug        Should compilation include the debug option?
  compile.deprecation  Should compilation include the deprecation option?
  compile.optimize     Should compilation include the optimize option?
-->

  <property name="compile.debug"       value="true"/>
  <property name="compile.deprecation" value="false"/>
  <property name="compile.optimize"    value="true"/>

<!-- ==================== External Dependencies =========================== -->

<!--
  Use property values to define the locations of external JAR files on which
  your application will depend.  In general, these values will be used for
  two purposes:
  * Inclusion on the classpath that is passed to the Javac compiler
  * Being copied into the "/WEB-INF/lib" directory during execution
    of the "deploy" target.

  Because we will automatically include all of the Java classes that Tomcat 4
  exposes to web applications, we will not need to explicitly list any of those
  dependencies.  You only need to worry about external dependencies for JAR
  files that you are going to include inside your "/WEB-INF/lib" directory.
-->

  <property name="jars"      value="${basedir}/jars"/>

<!-- ==================== Compilation Classpath =========================== -->

<!--
  Rather than relying on the CLASSPATH environment variable, Ant includes
  features that makes it easy to dynamically construct the classpath you
  need for each compilation.  The example below constructs the compile
  classpath to include the servlet.jar file, as well as the other components
  that Tomcat makes available to web applications automatically, plus anything
  that you explicitly added.
-->

  <path id="compile.classpath">
    <!-- Include all JAR files that will be included in /WEB-INF/lib -->
    <!-- Include all elements that Tomcat exposes to applications -->
	<fileset dir="${jars}">
		<include name="*.jar"/>
	</fileset> 
    <pathelement location="${catalina.home}/common/classes"/>
    <fileset dir="${catalina.home}/common/lib">
      <include name="*.jar"/>
    </fileset>
     <fileset dir="${catalina.home}/server/lib">
      <include name="*.jar"/>
    </fileset>
 </path>

<!-- ==================== Clean Target ==================================== -->

<!--
  The "clean" target deletes any previous "build" and "dist" directory,
  so that you can be ensured the application can be built from scratch.
-->

  <target name="clean"
   description="Delete old build and dist directories">
    <delete file="${web_inf.home}/web.xml"/>
    <delete dir="${build.home}"/>
 	<delete dir="${dist.home}"/>
	<delete dir="${catalina.home}/logs"/>
	<delete dir="${catalina.home}/work/Catalina/localhost"/>
 	<delete dir="${catalina.home}/webapps/${app.name}"/>
	<delete file="${catalina.home}/webapps/${app.name}.war"/>
	<delete dir="${catalina.home}/server/webapps/${app.name}"/>
  	<!-- 	<delete dir="${doc.home}"/> -->
    <mkdir dir="${build.home}"/>
    <mkdir dir="${dist.home}"/>
 	<mkdir dir="${doc.home}"/>
 	<mkdir dir="${catalina.home}/logs"/>
 </target>

  <target name="clean_all"
   description="Delete old build, dist directories and zips">
 	<delete dir="${zip_distributions.home}"/>
 	<mkdir dir="${zip_distributions.home}"/>
  </target>
	
<!-- ==================== Compile Target ================================== -->

<!--
  The "compile" target transforms source files (from your "src" directory)
  into object files in the appropriate location in the build directory.
  This example assumes that you will be including your classes in an
  unpacked directory hierarchy under "/WEB-INF/classes".
-->

  <target name="compile"
   description="Compile Java sources">
    <!-- Compile Java classes as necessary -->
    <mkdir    dir="${build.home}/WEB-INF/classes"/>
    <javac srcdir="${src.home}"
          destdir="${build.home}/WEB-INF/classes"
            debug="${compile.debug}"
      deprecation="${compile.deprecation}"
         optimize="${compile.optimize}">
        <classpath refid="compile.classpath"/>
    </javac>
  </target>

<!-- ==================== Dist Target ===================================== -->

<!--
  The "dist" target creates a binary distribution of your application
  in a directory structure ready to be archived in a tar.gz or zip file.
  Note that this target depends on two others:

  * "compile" so that the entire web application (including external
    dependencies) will have been assembled
-->

  
  <target name="dist_CBT" depends="clean, compile"
   	description="Create CBT binary distribution">
  	<antcall target="-WebXMLWindows" />
  	<antcall target="-CopyWebCBTToBuild" />
  	<antcall target="-WarBuild" />
<!--
    <CopyWebCBTToBuild/>
    <WarBuild/>
-->
  </target> 


  <target name="Copy war to Tomcat" 
   	description="Copy existing war to Tomcat - Does not rebuild">

    <!-- Install war to Tomcat -->
    <delete dir="${catalina.home}/webapps/${app.name}"/>
    <delete file="${catalina.home}/webapps/${app.name}.war"/>
    <copy file="${dist.home}/${app.name}-${app.version}.war" tofile="${catalina.home}/webapps/${app.name}.war"/>		 
  </target> 
  	
	
<!-- =================== Internal Tasks to prepare war file  ============ -->

	<!-- Copying web-unix.xml to web.xml -->
	<target name="-WebXMLunix">
		<copy file="${web.home}/WEB-INF/web-unix.xml" tofile="${web_inf.home}/web.xml"/>
	</target>
				
	<!-- Copying web-windows.xml to web.xml -->
	<target name="-WebXMLWindows">
		<copy file="${web.home}/WEB-INF/web-windows.xml" tofile="${web_inf.home}/web.xml"/>
	</target>

	<target name="-CopyWebToBuild" depends="prepare" >
		<copy todir="${build.home}">
			<fileset dir="${web.home}"/>
		</copy>
	</target>

	<target name="-CopyWebCBTToBuild" >
		<antcall target="-CopyWebToBuild" />
	    <copy  todir="${build.home}">
			<fileset dir="${web_CBT.home}"/>
	    </copy>
	</target>

	<target name="-WarBuild" >
	    <jar jarfile="${dist.home}/${app.name}-${app.version}.war"
	         basedir="${build.home}" />
	</target>
	
<!-- =================== Prepare Distributions ========================== -->
		
	<!-- Create unix WAR file -->
	<target name="BuildUnixWar" depends="clean, compile, -WebXMLunix, -CopyWebToBuild , -WarBuild"
			description="Create ${app.name}.war binary distribution">
	</target> 	
	
	<!-- Build unix Release -->
	<target name="BuildUnixWar_release" depends="BuildUnixWar"
			description="Builds Unix release for OWASP">
		<copy file="${dist.home}/${app.name}-${app.version}.war" tofile="${catalina.home}/webapps/${app.name}.war"/>
		<zip destfile="${zip_distributions.home}/Unix_${app.name}-${app.version}_Release.zip"
			basedir=""	
			includes="doc/, tomcat/, webgoat.sh, readme.txt"/>	
		<copy file="${dist.home}/${app.name}-${app.version}.war" tofile="${zip_distributions.home}/Unix_${app.name}-${app.version}.war"/>
	    <delete file="${catalina.home}/webapps/${app.name}.war"/>		 
	</target>

<!-- Create windows WAR file -->
	<target name="BuildWindowsWar" depends="clean, compile, -WebXMLWindows, -CopyWebToBuild, -WarBuild"
			description="Create ${app.name}.war binary distribution">
	</target> 	

<!-- Build windows release -->
	<target name="BuildWindowsWar_release" depends="BuildWindowsWar"
			description="Builds Windows release for OWASP">
		<copy file="${dist.home}/${app.name}-${app.version}.war" tofile="${catalina.home}/webapps/${app.name}.war"/>
		<zip destfile="${zip_distributions.home}/Windows_${app.name}-${app.version}_Release.zip"
			basedir=""	
		   	includes="doc/, java/, tomcat/, webgoat.bat, readme.txt"/>		
		<copy file="${dist.home}/${app.name}-${app.version}.war" tofile="${zip_distributions.home}/Windows_${app.name}-${app.version}.war"/>
	    <delete file="${catalina.home}/webapps/${app.name}.war"/>		 
	</target> 	
	
<!--Build developer release -->
	<target name="BuildDev_release" depends="clean"
		   description="Creates developer binary distribution for OWASP">   
		<zip destfile="${zip_distributions.home}/${app.name}-${app.version}_developer.zip"
		     basedir=""
	    	 includes="webgoat.bat, webgoat.sh, .project, .classpath, build.xml, deploy.wsdd, readme.txt, doc/, jars/, java/,
			lesson_plans/, src/, tomcat/, web/"/>
	</target> 
	
<!--Build all release -->
	<target name="_FIX_BuildALL_release" depends="clean_all, BuildDev_release, BuildUnixWar_release, BuildWindowsWar_release"
		   description="Creates all binary distributions for OWASP">
		<copy file="${basedir}/readme.txt" tofile="${zip_distributions.home}/readme.txt"/>
	</target> 

<!--Diff with current in sourceforge -->	
	<target name="Diff_CVS"
		description="exec task for shell script">
	</target>

<!-- ==================== Prepare Target ================================== -->

<!--
  The "prepare" target is used to create the "build" destination directory,
  and copy the static contents of your web application to it.  If you need
  to copy static files from external dependencies, you can customize the
  contents of this task.

  Normally, this task is executed indirectly when needed.
-->

	<target name="prepare">
		<!-- Create build directories as needed -->
		<mkdir  dir="${build.home}/WEB-INF/classes"/>  
		<!-- Copy src - DANGER: don't do this unless you want people to see your code -->
		<copy todir="${build.home}/src">
			<fileset dir="${src.home}"/>
		</copy>

		<copy todir="${build.home}/lesson_plans">
			<fileset dir="${basedir}/lesson_plans"/>
		</copy>
		<copy todir="${build.home}/doc">
			<fileset dir="${basedir}/doc"/>
		</copy>
	
		<!-- create the directory for the XSS and challenge stage 3 lessons -->
		<mkdir dir="${build.home}/messages"/>
		<!-- create the directory for persistent user data files -->
		<delete dir="${build.home}/users"/>
		<mkdir dir="${build.home}/users"/>
  	
		<!-- seed the directories so they get included in the war file -->
		<copy file="${basedir}/doc/New Lesson Instructions.txt" todir="${build.home}/users"/>
		<copy file="${basedir}/doc/New Lesson Instructions.txt" todir="${build.home}/messages"/>
  	 	

		<!-- Copy external dependencies as required -->
		<mkdir  dir="${build.home}/WEB-INF/lib"/>
		<copy todir="${build.home}/WEB-INF/lib">
			<fileset dir="${jars}"/>
		</copy>
		<!-- Copy application resources -->
		<copy  todir="${build.home}/WEB-INF/classes">
			<fileset dir="${src.home}" excludes="**/*.java"/>
		</copy>
	</target>


	<target name="Start Tomcat" 
			description="start the Tomcat server">
		<exec dir="${basedir}" executable="webgoat.bat" >
		</exec>  
	</target> 
	
	
<!-- ==================== Tomcat Targets ================================== -->
	
<!-- ==================== Install Target ================================== -->

<!--
  The "install" target tells the specified Tomcat 4 installation to dynamically
  install this web application and make it available for execution.  It does
  *not* cause the existence of this web application to be remembered across
  Tomcat restarts; if you restart the server, you will need to re-install all
  this web application.

  If you have already installed this application, and simply want Tomcat to
  recognize that you have updated Java classes (or the web.xml file), use the
  "reload" target instead.

  NOTE:  This target will only succeed if it is run from the same server that
  Tomcat is running on.

  NOTE:  This is the logical opposite of the "remove" target.
-->

  <target name="tomcat_install" depends="compile"
   description="Install application to servlet container">
    <install url="${manager.url}"
        username="${manager.username}"
        password="${manager.password}"
            path="${app.path}"
             war="file://${build.home}"/>
  </target>

<!-- ====================== List Target =================================== -->

<!--
  The "list" target asks the specified Tomcat 4 installation to list the
  currently running web applications, either loaded at startup time or
  installed dynamically.  It is useful to determine whether or not the
  application you are currently developing has been installed.
-->

  <target name="tomcat_list"
   description="List installed applications on servlet container">
    <list    url="${manager.url}"
        username="${manager.username}"
        password="${manager.password}"/>
  </target>



  		
<!-- ==================== Reload Target =================================== -->

<!--
  The "reload" target tells the specified Tomcat 4 installation to dynamically
  reload this web application, to reflect changes in the underlying classes or
  the "web.xml" deployment descriptor.
-->

  <target name="tomcat_reload" depends="compile"
   description="Reload application on servlet container">

    <!-- FIXME: Tomcat does not refresh the class files on the load -->
    <!-- FIXME: The install task copies the entire webgoat build to the webabbs directory -->
    <copydir  dest="${catalina.home}/webapps/${app.name}/WEB-INF/classes" 
    	      src="${build.home}/WEB-INF/classes">
    </copydir>
    <copydir  dest="${catalina.home}/webapps/${app.name}/lesson_plans" 
    	      src="${build.home}/lesson_plans">
    </copydir>
  	<reload url="${manager.url}"
       username="${manager.username}"
       password="${manager.password}"
           path="${app.path}"/>
  </target>

<!-- ==================== Remove Target =================================== -->

<!--
  The "remove" target tells the specified Tomcat 4 installation to dynamically
  remove this web application from service.

  NOTE:  This is the logical opposite of the "install" target.
-->

<target name="tomcat_remove"
   description="Remove application on servlet container">
    <remove url="${manager.url}"
       username="${manager.username}"
       password="${manager.password}"
           path="${app.path}"/>
  </target>
</project>



